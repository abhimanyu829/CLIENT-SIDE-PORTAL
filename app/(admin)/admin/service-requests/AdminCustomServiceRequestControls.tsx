"use client"
import { useEffect, useState } from "react"
import { Settings2 } from "lucide-react"

export default function AdminCustomServiceRequestControls() { const [enabled, setEnabled] = useState(true); const [path, setPath] = useState("/request-service"); const [saved, setSaved] = useState(false); useEffect(() => { void fetch("/api/admin/custom-service-portal/settings").then((response) => response.json()).then((payload) => { if (payload?.success) { setEnabled(payload.data.isEnabled); setPath(payload.data.publicPath) } }) }, []); async function save() { const response = await fetch("/api/admin/custom-service-portal/settings", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ isEnabled: enabled, publicPath: path }) }); setSaved(response.ok) } return <div className="flex flex-wrap items-center gap-3 rounded-2xl border border-border bg-card p-4 shadow-sm"><Settings2 className="h-5 w-5 text-amber-800" /><label className="flex items-center gap-2 text-sm font-medium text-foreground"><input type="checkbox" checked={enabled} onChange={(event) => setEnabled(event.target.checked)} />Portal enabled</label><select value={path} onChange={(event) => setPath(event.target.value)} className="rounded-lg border border-border bg-background px-3 py-2 text-sm text-foreground"><option value="/request-service">/request-service</option><option value="/custom-service">/custom-service</option></select><button onClick={save} className="rounded-lg bg-amber-800 px-3 py-2 text-sm font-semibold text-white">Save portal settings</button>{saved && <span className="text-sm text-emerald-700">Saved</span>}</div> }
